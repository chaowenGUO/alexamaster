plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    compile 'com.microsoft.playwright:playwright:1.9.0-alpha-0'
}

task copyDependencies(type: Copy) {
    from configurations.default into 'copyDependencies'
}
