plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    compile 'com.microsoft.playwright:playwright:latest.release'
}

task copyDependencies(type: Copy) {
    from configurations.default into '.'
}
